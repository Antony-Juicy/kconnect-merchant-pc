import React from 'react';
import NormalLayout from '@/components/Layout/NormalLayout';

const Agreement: React.FC = () => {

  return <NormalLayout layoutStyle={{ padding: '0 0 24px 0' }} />;
};

export default Agreement;
